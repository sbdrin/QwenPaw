- 使用#[tauri::command]属性标记供前端调用的Rust函数
- 通过tauri::State<'_, T>参数访问共享状态对象
- 使用Emitter trait的emit方法在Rust和前端之间发送事件
- 采用模块化设计，将不同功能拆分为独立的Rust模块文件
- 使用Atomic类型和Mutex实现跨线程的安全状态共享
- 通过generation计数器防止过期的异步操作更新当前状态