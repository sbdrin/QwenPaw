模块采用组件化架构，核心是工具卡片系统。Chat/ToolCards子目录包含三类结构：cards（具体工具卡片实现）、shared（共享组件和类型定义）、adapters（适配器用于兼容不同版本）。通过registerBuiltinCards统一注册所有内置卡片到插件系统，支持ChatV1和ChatV2两种渲染模式。每个工具卡片遵循统一的BuiltinCardProps接口，接收content和isStreaming参数。