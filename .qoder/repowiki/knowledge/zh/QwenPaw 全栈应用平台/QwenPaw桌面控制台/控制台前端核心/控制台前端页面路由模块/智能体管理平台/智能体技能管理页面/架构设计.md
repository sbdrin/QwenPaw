采用React函数组件架构，使用自定义Hook(useSkills, useSkillsPage)管理业务逻辑和状态。UI层分为卡片视图(SkillCard)和列表视图(SkillListItem)，通过ViewMode切换。组件结构清晰分离：index.tsx为页面入口，components目录包含可复用UI组件，useSkills.ts处理API交互和缓存失效，useSkillsPage.tsx整合页面状态。采用Ant Design组件库构建界面，支持移动端响应式设计。