- 组件层：主页面(index.tsx)使用Ant Design的Tab组件组织已安装、官方和市场三个插件视图
- Hooks层：分离业务逻辑到独立的hooks目录，包括usePluginManager(核心管理)、useMarketPlugins(市场数据)、useOfficialPlugins等
- 组件层：可复用UI组件放置在components目录，如安装模态框、插件列表等
- 样式层：采用CSS Modules(.module.less)实现样式隔离
- 数据流：通过ahooks的useRequest处理API请求，配合国际化(i18n)实现多语言支持