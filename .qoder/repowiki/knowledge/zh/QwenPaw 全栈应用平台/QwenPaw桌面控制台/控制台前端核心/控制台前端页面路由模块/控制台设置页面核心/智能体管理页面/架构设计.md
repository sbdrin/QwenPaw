采用React函数组件架构，使用Ant Design UI库构建界面。内部按职责分层：主页面(index.tsx)负责状态管理和用户交互，自定义Hook(useAgents.ts)封装数据获取和操作逻辑，组件目录(components/)存放可复用UI组件如表格(AgentTable)和模态框(AgentModal)。通过dnd-kit实现拖拽排序功能，集成国际化(i18n)支持多语言，使用Zustand store管理全局智能体状态。