采用组件化架构，以AgentScopeRuntimeWebUI为核心渲染引擎，通过自定义sessionApi与后端API集成。内部按功能划分为components子目录（会话抽屉、操作组等UI组件）、hooks子目录（会话创建逻辑）和sessionApi子目录（会话数据管理）。使用虚拟列表优化大量会话的渲染性能，实现后台消息队列机制支持离线发送，通过LRU缓存减少重复网络请求。整体遵循React函数式组件模式，结合Ant Design和自定义样式系统。