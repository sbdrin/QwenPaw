模块采用单例模式设计，核心为ApprovalService类管理所有审批请求。内部包含三个主要组件：models.py定义审批数据模型（如ApprovalRequestSummary），service.py实现审批服务逻辑（创建、解析、等待审批等），driver_gate.py作为驱动策略与审批服务的桥接层。服务使用asyncio.Future实现异步审批决策，并通过锁保护的内存字典存储待处理审批记录。架构支持基于会话ID、代理ID和根会话ID的多维度审批查询和管理。