- 使用dataclass定义不可变的数据模型，配合field(default_factory=dict)处理可变默认值
- 采用TYPE_CHECKING导入避免循环依赖，将类型导入放在if TYPE_CHECKING块中
- 使用uuid.uuid4()生成唯一请求ID，配合日志记录中的ID截断显示（[:8]）
- 通过asyncio.Lock保护共享状态访问，确保并发安全的操作
- 采用Future模式实现异步结果等待，使用asyncio.wait_for设置超时