采用单一服务容器模式，通过AppServiceManager类持有三个跨工作区协调器：task_tracker、tool_coordinator和approval_coordinator。使用__slots__限制实例属性，通过FastAPI lifespan进行生命周期管理，在启动时调用start方法，关闭时调用stop方法。ApprovalCoordinator作为ApprovalService单例的外观包装器，支持过渡期兼容性。