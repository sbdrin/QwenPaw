- 使用Path参数处理动态客户端键路径，将路径参数放在最后注册以避免路由冲突
- 采用嵌入式请求体模型（Body(embed=True)）处理简单数据类型参数
- 通过中间件函数（如_mcp_service、_agent_for_request）封装通用服务获取逻辑
- 使用HTMLResponse返回前端交互页面，包含JavaScript用于窗口通信和自动关闭
- 采用异步函数处理所有请求，通过HTTPException统一处理错误响应
- 使用Pydantic模型定义请求和响应的数据结构，确保类型安全和验证