- ContextManager协议定义了可注入的上下文管理策略接口
- ScrollContextManager实现了写入缓存（write-through）和驱逐索引（eviction-index）的上下文管理
- 持久化存储使用HistoryStore将对话历史存储到SQLite数据库
- 支持分层压缩：保留最近尾部和活动回合，将驱逐的中间部分折叠到驱逐索引中
- 提供序列化工具将消息转换为持久化条目
- 包含内存空间管理和同步机制