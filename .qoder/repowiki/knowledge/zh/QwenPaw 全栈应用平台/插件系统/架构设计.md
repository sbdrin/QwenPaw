插件按类型分为channel、tool、middleware等类别，每个插件通过plugin.json定义元数据和后端入口点。插件采用统一的JSON配置格式，包含ID、版本、依赖和入口文件信息，实现模块化加载机制。
- 插件类型包括：channel（如Azure Bot）、tool（如GPT图像生成）、general（如中间件）
- 每个插件独立部署，通过entry.backend指定Python入口文件
- 支持国际化描述和版本约束管理